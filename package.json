{
  "name": "blind-escape-room",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-prod": "tsc && vite build --base=/blind-escape-room/",
    "build-itch": "tsc && vite build --base=/html/12955477/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "prettier": "^3.5.2",
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "kontra": "^10.0.2",
    "tone": "^15.0.4"
  }
}
